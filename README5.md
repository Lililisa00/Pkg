📚 Теоретическая часть

1. Алгоритм Лианга-Барски

Назначение: Отсечение отрезков прямоугольным окном.

Математические основы:
Отрезок задается параметрически:

text
x = x1 + u * (x2 - x1)
y = y1 + u * (y2 - y1), где u ∈ [0, 1]
Параметры для границ окна:

Левая граница: u = (xmin - x1) / (x2 - x1)
Правая граница: u = (xmax - x1) / (x2 - x1)
Нижняя граница: u = (ymin - y1) / (y2 - y1)
Верхняя граница: u = (ymax - y1) / (y2 - y1)
Алгоритм:

Инициализировать u1 = 0, u2 = 1
Для каждой границы окна вычислить p и q
нам надо было только лианга барски
Лабораторная работа: Алгоритм отсечения Лианга-Барски

📋 Содержание

Введение
Теоретические основы алгоритма Лианга-Барски
Математическая модель
Реализация алгоритма
Формат входных данных
Примеры тестов
Результаты тестирования
Заключение
🎯 Введение

Цель работы: Изучение и реализация алгоритма отсечения Лианга-Барски для отсечения отрезков прямоугольным окном.

Задачи:

Изучить теоретические основы алгоритма Лианга-Барски
Реализовать алгоритм на языке программирования Python
Разработать систему ввода/вывода данных
Провести тестирование на различных наборах данных
Визуализировать результаты отсечения
📚 Теоретические основы алгоритма Лианга-Барски

Описание алгоритма

Алгоритм Лианга-Барски — это эффективный параметрический алгоритм для отсечения отрезков прямоугольным окном. Алгоритм был предложен в 1984 году Юнг-Лиангем Ляном и Брайаном А. Барски.

Преимущества алгоритма:

Высокая эффективность — требует меньше вычислений, чем алгоритм Коэна-Сазерленда
Параметрический подход — работает с параметрическим представлением отрезков
Универсальность — корректно обрабатывает все случаи расположения отрезков
Простота реализации — основан на простых математических операциях
🔢 Математическая модель

Параметрическое представление отрезка

Отрезок от точки P₁(x₁, y₁) до точки P₂(x₂, y₂) представляется в параметрической форме:

text
x = x₁ + u * Δx
y = y₁ + u * Δy
где:
Δx = x₂ - x₁
Δy = y₂ - y₁
u ∈ [0, 1] — параметр
Уравнения границ окна

Прямоугольное окно задается координатами:

xmin — левая граница
xmax — правая граница
ymin — нижняя граница
ymax — верхняя граница
Неравенства для видимой части отрезка:

xmin ≤ x₁ + u * Δx ≤ xmax
ymin ≤ y₁ + u * Δy ≤ ymax

p₁ = -Δx, q₁ = x₁ - xmin  (левая граница)
p₂ =  Δx, q₂ = xmax - x₁  (правая граница)
p₃ = -Δy, q₃ = y₁ - ymin  (нижняя граница)
p₄ =  Δy, q₄ = ymax - y₁  (верхняя граница)

Выводы

Алгоритм Лианга-Барски является эффективным и надежным методом отсечения отрезков прямоугольным окном
Параметрический подход позволяет точно определять видимые части отрезков
Реализация алгоритма показала корректную работу на всех тестовых наборах данных
Алгоритм эффективно обрабатывает все возможные случаи расположения отрезков
